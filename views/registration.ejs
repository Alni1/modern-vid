<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/stylesheets/style.css" rel="stylesheet"/>

    <title><%= title %></title>
</head>
<body style="margin:0;">
    <%- include('partials/header', { user: null, query: '' }) %>

<div class="container" style="margin-top: 95px;">
    <h1>Registration</h1>
    <hr>

    <% if (typeof error !== 'undefined' && error) { %>
        <p style="color: red; text-align: center;"><%= error %></p>
    <% } %>

    <% if (typeof validationError !== 'undefined' && validationError) { %>
        <ul style="color: red; list-style-type: none; padding-left: 0; text-align: center;">
            <% Object.values(validationError).forEach(function(errMsg) { %>
                <li><%= errMsg %></li>
            <% }); %>
        </ul>
        <% } %>
    
        <form action="/registration" method="POST" id="gov1" style="width: 100%;">
            <div class="form-group">
                <label for="login"><b>Login</b></label>
                <input type="text" placeholder="Enter your login" name="login" id="login" value="<%= typeof loginValue !== 'undefined' ? loginValue : '' %>" required>
            </div>
            
            <div class="form-group">
                <label for="username"><b>Username</b></label>
                <input type="text" placeholder="Enter your username" name="username" id="username" value="<%= typeof usernameValue !== 'undefined' ? usernameValue : '' %>" required>
            </div>
            
            <div class="form-group">
                <label for="password"><b>Password</b></label>
                <input type="password" placeholder="At least 6 characters" name="password" id="password" required>
            </div>
            <hr>
            <button type="submit" class="registerbtn">Register</button>
        </form>
</div>

<div class="container signin" style="margin-top: 20px; text-align: center;">
    <p>Already have an account? <a href="/login">Login</a>.</p>
</div>

<%# <script src="/pages/index.js"></script> -- Consider if this script is needed and path is correct %>
</body>
</html>